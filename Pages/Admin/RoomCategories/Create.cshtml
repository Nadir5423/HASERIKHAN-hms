@page
@model HMS.Pages.Admin.RoomCategories.CreateModel
@{
    ViewData["Title"] = "Add Category";
}

<div class="row justify-content-center mt-4">
    <div class="col-md-8">
        <div class="card card-enterprise shadow-sm animate-enter delay-100">
            <div class="card-body p-5">
                <div class="mb-4 pb-3 border-bottom">
                    <h2 class="text-headline mb-1"><i class="fa-solid fa-folder-plus me-2" style="color: var(--clr-primary-500);"></i> Add Room Category</h2>
                    <p class="text-body text-muted mb-0">Create a new pricing tier and guest limit definition</p>
                </div>

                <form method="post">
                    <!-- Category Name -->
                    <div class="mb-3">
                        <label asp-for="Category.CategoryName" class="form-label-enterprise">Category Name</label>
                        <input asp-for="Category.CategoryName" class="form-control form-control-enterprise" placeholder="Standard, Deluxe, Suite, etc." required />
                        <span asp-validation-for="Category.CategoryName" class="text-danger font-size-xs"></span>
                    </div>

                    <div class="row">
                        <!-- Base Price -->
                        <div class="col-md-6 mb-3">
                            <label asp-for="Category.BasePrice" class="form-label-enterprise">Base Price ($)</label>
                            <input asp-for="Category.BasePrice" type="number" step="0.01" class="form-control form-control-enterprise" placeholder="100.00" required />
                            <span asp-validation-for="Category.BasePrice" class="text-danger font-size-xs"></span>
                        </div>
                        <!-- Max Occupancy -->
                        <div class="col-md-6 mb-3">
                            <label asp-for="Category.MaxOccupancy" class="form-label-enterprise">Max Occupancy (Guests)</label>
                            <input asp-for="Category.MaxOccupancy" type="number" min="1" class="form-control form-control-enterprise" placeholder="2" required />
                            <span asp-validation-for="Category.MaxOccupancy" class="text-danger font-size-xs"></span>
                        </div>
                    </div>

                    <!-- Description -->
                    <div class="mb-4">
                        <label asp-for="Category.Description" class="form-label-enterprise">Description (Optional)</label>
                        <textarea asp-for="Category.Description" class="form-control form-control-enterprise" rows="3" placeholder="Enter description..."></textarea>
                    </div>

                    <!-- Actions -->
                    <div class="d-flex gap-3 justify-content-end mt-4">
                        <a asp-page="./Index" class="btn btn-outline-secondary px-4"><i class="fa-solid fa-arrow-left me-1"></i> Cancel</a>
                        <button type="submit" class="btn btn-primary px-4"><i class="fa-solid fa-floppy-disk me-1"></i> Save Category</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

